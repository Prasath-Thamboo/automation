# Expo / React Native (Metro) ne sait pas résoudre les dépendances transitives
# sous le layout "isolated" de pnpm : Metro attend un node_modules aplati.
# hoisted = un seul node_modules à la racine, comme npm/yarn.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
