/**
 * Composants et thème mobile partagés (Expo / React Native).
 * Séparé de `@tando/ui` (web) : on partage les types, la copy et les appels API,
 * pas les composants web. Les composants natifs arrivent au Lot 7.
 */
export { theme, type Theme } from "./theme";
