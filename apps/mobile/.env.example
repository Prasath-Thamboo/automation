# apps/mobile — variables Expo (préfixe EXPO_PUBLIC_ pour être exposées au bundle).
# En dev sur simulateur iOS, localhost fonctionne. Sur un appareil Android réel,
# remplacer par l'IP de la machine (ex. http://192.168.1.20:3333).
EXPO_PUBLIC_API_URL=http://localhost:3333
